# AnRAG benchmark — Python >= 3.11
#
# Setup:
#   pip install -r requirements.txt
#   pip install -e .
#
# Quick run (sample corpus):
#   python -m anrag.cli ablation benchmarks/sample/corpus.txt --qa benchmarks/sample/qa.jsonl
#   powershell -File scripts/run_benchmark.ps1
#
# Official benchmarks:
#   python -m anrag.cli ablation path/to/beir/fiqa --format beir
#   python -m anrag.cli ablation path/to/hotpot_dev.jsonl --format hotpotqa
#   python -m anrag.cli ablation path/to/kilt.jsonl --format kilt

# Core (required)
pydantic>=2.0
pydantic-settings>=2.0
numpy>=1.24
scikit-learn>=1.3
rank-bm25>=0.2.2
faiss-cpu>=1.7.4
PyMuPDF>=1.23

# Semantic embeddings (strongly recommended for benchmark)
sentence-transformers>=2.2.2

# Optional: API / upload
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6

# Optional: tests
pytest>=7.4
